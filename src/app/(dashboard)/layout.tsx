export default function DashboardLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <div>
            <div>Generec Dashboard Layout</div>
            {children}
        </div>
    );
}
   